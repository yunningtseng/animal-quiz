export interface User {
  id: string;
  fbId?: string;
  googleId?: string;
  name?: string;
  bestScore?: number;
  bestScoreResponseId?: string;
  totalTime?: number;
  mode?: string;
  // best?: {
  //   normal:
  // }
}
